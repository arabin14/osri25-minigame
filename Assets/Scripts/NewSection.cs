using UnityEngine;

/*public class NewSection : MonoBehaviour
{
    //[SerializeField] private Transform previousSection;
    [SerializeField] private Transform nextSection; 
    [SerializeField] private CameraController cam;

    private void OnTriggerEnter2D(Collider2D collision) 
    {
        if (collision.tag == "Player")
        {
            // moves camera to the right, player coming from the left
            if (collision.transform.position.x < transform.position.x)
                cam.MoveToNewSection(nextSection);
        }
        // does not handle moving camera to the left 
    }
} */
